/// <reference types="vite/client" />

// Injected by `define` in vite.config.ts. Captured at build time so error
// reports and bug-report screenshots can be tied back to a specific commit.
declare const __COMMIT__: string;
declare const __BUILD_TIME__: string;
