$kind: http-request
url: "{{BASE_URL}}/posts/{{POST_ID}}/comments/{{COMMENT_ID}}/like"
method: POST
headers:
  Authorization: Bearer {{TOKEN}}
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
